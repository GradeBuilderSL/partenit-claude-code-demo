# Your LLM API Key (Gemini or OpenAI)
LLM_PROVIDER=gemini
LLM_API_KEY=your_llm_api_key_here

# ============================================================================
# Notes
# ============================================================================

# This .env file is NOT used by the frontend (browser can't read .env files)
# It's provided as a reference template only
# 
# The actual configuration is done through the browser UI:
# 1. Open the game in your browser
# 2. Click the "Config" button
# 3. Enter your API keys
# 4. Keys are stored in browser localStorage
#
# This keeps your keys secure and allows easy configuration
